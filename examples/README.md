# Synthetic demo fixtures

Everything under this directory is fictional test data for the EvidenceOps demo. It must not be presented as evidence about a real organization.

- `questionnaire.csv` is a buyer security questionnaire.
- `evidence/` contains the fictional vendor's approved evidence set.
- The set intentionally includes one conflict and several unanswered questions so the review workflow is visible.
